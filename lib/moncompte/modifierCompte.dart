import 'package:flutter/material.dart';

class ModifierCompte extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Modifier mon compte'),
      ),
      body: Center(
        child: Text("ok"),
      ),
    );
  }
}
